#Replace the double space from problem 3 with single spaces.
name = "Saurabh is a good  boy"
print(name.replace("  ", " ")) #Strings are immutable which means you cannot change them by running function on them